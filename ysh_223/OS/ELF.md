> **ELF**
[[OS]]
---

⇒ 리눅스 기반 시스템의 기본 바이너리 형태 (바이너리 형태)

⇒ 실행 가능한 바이너리 파일, 목적 파일, 공유 라이브러리, 코어 덤프 등에서 사용되는 형식이다

### `PT_LOAD`

⇒ 파일의 특정 구간을 가상 메모리에 매핑해야 함을 OS 실행한다

OS는 PT_LOAD를 보고

- 파일 오프셋 + 크기 확인
- 가상 주소 지정
- 접근 권한 설정
- `mmap()` 으로 매핑