Makefile에 경로와 빌드 진행 방식을 기술했다면, 이를 진행할 컴파일러를 선정하고, 컴파일해야한다. 컴파일러는 어떤 종류들이 있고, 이 프로젝트에서는 어떤 컴파일러를 사용할까?

- GCC
- G++
- Clang
- MSVC

등 많은 컴파일러가 있고, Linux/임베디드에서 가장 많이 사용하는 gcc를 사용할 것이다.

---

### GNU Compiler Collection

GNU프로젝트는 사용자의 SW를 자유롭게 실행할 수 있는 운영체제 환경을 만드는 프로젝트이다. gcc는 GNU 프로젝트의 일환으로, C, C++, Java, Fortran 등 여러 언어를 컴파일 할 수 있다. 

GCC 컴파일러는 우리가 아는

`전처리 → 컴파일 → 어셈블 → 링킹` 과정으로 이뤄진다. 

( c → s → i → i → elf)

---

GCC는 다양한 이유에서 가장 많이 사용되는 컴파일러이다.

- 무료 소스 / 오픈 소스
- 다양한 CPU arch 지원
- 임베디드 SDK 등이 GCC 지원
- 좋은 성능
- Linux에 적합한 컴파일러
